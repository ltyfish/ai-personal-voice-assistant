node_modules
.next
*.tsbuildinfo
.env
.env.local
.env*.local
/jarvis-pet.env
*.log
.DS_Store
.vercel
db/migrations/meta
scripts/bridge/.bridge-token
scripts/migrated-keys.json

# Abilities-tab keyword/ability requests (runtime, local only)
keyword-requests.md

# Separate prod build output (see next.config.mjs)
.next-build/

graphify-out/
.superpowers/
# Bridge browser profile (runtime Chrome data, never commit)
scripts/bridge/.browser-profile/

# Windows desktop app build outputs
desktop/dist/
desktop/release/

.vercel
AGENTS.md
CLAUDE.md
nextdev.log
nextdev.err.log

node_modules
.next
*.tsbuildinfo
.env
.env.local
.env*.local
*.log
.DS_Store
.vercel
db/migrations/meta
scripts/bridge/.bridge-token
scripts/migrated-keys.json

# Abilities-tab keyword/ability requests (runtime, local only)
keyword-requests.md

# Separate prod build output (see next.config.mjs)
.next-build/

graphify-out/
# Bridge browser profile (runtime Chrome data, never commit)
scripts/bridge/.browser-profile/

.vercel

AGENTS.md
CLAUDE.md
nextdev.log
nextdev.err.log
